<?xml version="1.0" encoding="utf-8"?>
<!--
    Modified by the Trawl project on 2026-08-25 (GPL-3.0 section 5(a)).
    Change: added the SYSTEM_ALERT_WINDOW permission below, for Trawl's floating
    download bubble. Everything else in this file is unchanged from Seal Plus.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        android:minSdkVersion="33" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!--
        WAKE_LOCK: lets DownloaderV2Impl hold a PARTIAL_WAKE_LOCK while a download is actively
        running, so Doze mode cannot suspend the CPU mid-download and stall/kill the yt-dlp
        process. This is the fix for downloads that silently fail while the app is backgrounded
        for a while but work again as soon as the app is reopened/resumed — reopening the app
        was accidentally "fixing" the underlying Doze CPU-suspend issue by waking the device.
    -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
        SYSTEM_ALERT_WINDOW ("Display over other apps") - ADDED BY TRAWL, 2026-08-25.

        Backs the floating download bubble: a draggable overlay showing each running
        download as a progress ring, above other apps.

        This is a *special* permission, not a normal runtime one. It cannot be requested
        with the standard permission dialog - the user must be sent to
        Settings.ACTION_MANAGE_OVERLAY_PERMISSION, and the grant is read back with
        Settings.canDrawOverlays(context). Some OEM ROMs refuse or silently revoke it.

        Therefore the bubble is strictly an ENHANCEMENT. Download progress must remain
        fully visible in the DownloadService foreground notification and on the home
        screen when this permission is absent. Never make the bubble the only place
        progress is shown. See DECISIONS.md D-08.
    -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />

            <category android:name="android.intent.category.BROWSABLE" />

            <data android:scheme="https" />
        </intent>

        <!--
            Android 11+ (API 30) package visibility: PackageManager.resolveActivity() returns
            null for any package not declared here, so BatteryUtil.isIntentResolvable() would
            always return false for manufacturer-specific battery-management apps and fall back
            to the generic Settings page instead of opening the OEM-specific battery screen.
        -->

        <!-- Xiaomi / MIUI / Redmi / POCO -->
        <package android:name="com.miui.securitycenter" />
        <package android:name="com.miui.powerkeeper" />

        <!-- OPPO / Realme / ColorOS -->
        <package android:name="com.coloros.oppoguardelf" />
        <package android:name="com.heytap.manager" />
        <package android:name="com.coloros.safecenter" />
        <package android:name="com.coloros.oppomanager" />

        <!-- Vivo / iQOO / FuntouchOS -->
        <package android:name="com.vivo.permissionmanager" />
        <package android:name="com.iqoo.secure" />

        <!-- Huawei / Honor / EMUI -->
        <package android:name="com.huawei.systemmanager" />

        <!-- Samsung / One UI -->
        <package android:name="com.samsung.android.lool" />
    </queries>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:targetApi="tiramisu">
        <activity
            android:name=".CrashReportActivity"
            android:exported="false"
            android:label="CrashReportActivity"
            android:theme="@style/Theme.SealPlus" />
        <activity
            android:name=".QuickDownloadActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:label="@string/title_activity_share"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.SealPlus.Dialog">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:mimeType="video/*" />
                <data android:mimeType="audio/*" />
            </intent-filter>
        </activity>

        <service
            android:name=".DownloadService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <!--
                Required by Google Play policy for the "specialUse" foreground service type
                (Android 14+ / API 34+ enforcement). Without this <property>, Play Console flags
                the app during review for an undeclared special-use justification. This does not
                affect behavior on-device, only the Play Store submission/review process.
            -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps a video/audio download running via yt-dlp while the app is in the background" />
        </service>

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="unspecified"
            android:theme="@style/Theme.SealPlus">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:mimeType="video/*" />
                <data android:mimeType="audio/*" />
            </intent-filter>
        </activity>

        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <receiver
            android:name=".NotificationActionReceiver"
            android:exported="false" />
    </application>

</manifest>